# Environment variables for development

# Server Configuration
DEBUG=false
HOST=0.0.0.0
PORT=8000

# Model Configuration
MODEL_FOLDER=./fine_tuned_model
MAX_LENGTH=200
DEVICE=  # Leave empty to auto-detect (GPU if available, else CPU)

# CORS Origins
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
